{
  "name": "gamevault-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:start --prefix backend\" \"npm:dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
